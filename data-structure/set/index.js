const set = new Set([1, 2, 3]);

// set.add(4);
// console.log(set.has(4)); // check if the set has this value

// set.delete(3); // delete the value
// console.log(set.size); // check the size or the length of the set
// set.clear(); // delete all values in the set

// for (const item of set) {
//   console.log(item);
// }
